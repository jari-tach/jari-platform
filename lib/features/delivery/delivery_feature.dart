/// Feature marker for Delivery Request Lifecycle (PHASE 2.5).
///
/// Domain contracts live under [domain]. Data and presentation layers are
/// added in later increments — this marker must not grow business logic.
class DeliveryFeature {
  /// Creates the feature marker.
  const DeliveryFeature();
}
